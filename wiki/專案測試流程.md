---
title: 專案測試流程
description: 前後端分離專案的測試分層總覽：四層各自的射程、一個測試該歸哪一層的判準，以及兩端共通的驗收紀律與停止條件
created: 2026-08-08
updated: 2026-08-14
parent: "[[wiki/01.index]]"
tags:
  - testing
  - vue
  - laravel
  - coding-agent
---

> **總覽** ｜ [[專案測試流程-前端-Vue|前端 Vue]] ｜ [[專案測試流程-後端-Laravel|後端 Laravel]]

測試流程是**堆起來的**，不是一次設計好的。每一層都要在下一層開始之前真的在跑，否則後面那些昂貴的手段（mutation、property-based）建在流沙上——這點在 [[用測試約束-AI-產碼]] 第六節的順序建議裡是核心約束。

**本頁是總覽**：講分層、判準與兩端共通的紀律。各端的具體做法分開走，因為 Vue 與 Laravel 的工具、檔案結構與坑都不一樣，混在一頁讀起來會一直左右橫跳：

- [[專案測試流程-前端-Vue]] — Vitest、composable 與元件的分界、`mount()` 的成本
- [[專案測試流程-後端-Laravel]] — `Unit`／`Feature` 兩個 testsuite、資料庫的隔離

## 適用背景

前後端分離，前端 Vue、後端 Laravel，兩端各自獨立部署、透過 HTTP API 溝通。

```
   ┌─────────────────────┐                  ┌─────────────────────┐
   │  Vue (SPA)          │    HTTP / JSON   │  Laravel (API)      │
   │    components       │ ◀──────────────▶ │    Controller       │
   │      composables    │                  │      Service        │
   │        utils        │                  │        Model / DB   │
   └─────────────────────┘                  └─────────────────────┘
            前端 repo                              後端 repo
```

分離架構對測試流程的關鍵影響，不是「工作量變兩倍」，而是**多出一個誰都不擁有的問題**：兩端各自全綠，接起來仍然可能是壞的。中間那條 HTTP 邊界是分離架構最容易出事的地方，也是第三層存在的理由。

## 四層的射程

前兩層在單邊之內，後兩層跨越 HTTP 邊界：

```
第 1 層 · 單元測試 ── 不啟動框架、毫秒級
    ├ 前端：utils、純 composable
    └ 後端：Service、Value Object

第 2 層 · 框架內測試 ── 啟動框架，但仍在單邊之內
    ├ 前端：掛載元件、驗互動
    └ 後端：Feature 測試、打 route、碰 DB

第 3 層 · 契約測試 ── 分離架構特有，跨越 HTTP 邊界
    └ 前端假設的 API 形狀  ⟷  後端實際回的形狀

第 4 層 · 端到端 ── 真瀏覽器 + 真後端，最慢最脆
    └ 只留關鍵路徑
```

**第 1、2 層全綠不代表系統是對的。** 兩端可以各自完美，而前端等的欄位叫 `total`、後端回的叫 `total_amount`——這種錯不在任何一邊的測試射程內，只有第 3 層抓得到。單體專案沒有這條邊界，所以也沒有這一層的成本；這是分離架構自己買單的部分。

> 精緻版架構圖（四層射程／抽離前後測試要穿過什麼／判準流程）在 [`docs/architecture/testing-layers-vue-laravel.html`](../docs/architecture/testing-layers-vue-laravel.html)，單檔可直接用瀏覽器開。

## 一個測試該歸哪一層

不必憑感覺分類。三個問題依序問下去，每個測試都有唯一歸屬：

```
一個測試
   │
   ├─ 拿掉框架還能跑嗎？ ── 能 ──▶ 第 1 層 · 單元
   │        │
   │        └ 不能
   │            │
   │            ├─ 只在單邊之內嗎？ ── 是 ──▶ 第 2 層 · 框架內
   │            │        │
   │            │        └ 否
   │            │            │
   │            │            ├─ 需要真瀏覽器嗎？ ── 否 ──▶ 第 3 層 · 契約
   │            │            └────────────────── 是 ──▶ 第 4 層 · 端到端
```

**第一個問題就能擋掉大部分的混亂。** 「拿掉框架還能不能跑」在兩端都有現成的檢查訊號，各自寫在子頁裡。

分層不是為了分類學上的整齊，而是為了**跑的頻率不同**：第 1 層每次存檔都該跑得起，第 4 層通常只在合併前跑。層次混在一起，最慢的那個會拖垮整批，最後的下場是整批都不跑。

## 兩端共通的紀律

以下四條與框架無關，兩端都適用，子頁不再重複。

### 起點不是「怎麼寫測試」，是「有沒有東西可以測」

絕大多數專案卡在這裡而不自知，而且 Vue 與 Laravel 卡的是**同一個坑**：框架讓人很容易把判斷寫在「必須啟動框架才能執行」的位置，而那正是不可測的地方。

```
❌ 判斷長在框架內部              要測它，得先把整個框架跑起來
     框架外殼
       └ 判斷

✅ 判斷脫離框架                  框架那側只剩「呼叫它、把結果交出去」
     框架外殼 ──呼叫──▶ 判斷     沒有判斷，因此不需要在第一層測它
```

**判準**：一個函式若要測它得先準備環境（掛載元件、啟動框架、連資料庫、開網路），它就還不是單元。先抽，再測。兩端各自怎麼抽，見子頁。

### 斷言要具體

`toBeTruthy()`、`assertNotNull()`、`length > 0` 這類弱斷言是 AI 產測試最高頻的失效模式（見 [[用測試約束-AI-產碼]] 第一節），它們看起來綠，但容忍錯誤的值。要斷言**確切等於什麼**。

每個函式起手三種案例就夠了，不必更多：

| 案例 | 問的問題 |
|---|---|
| 正常輸入 | 該成功的時候有沒有成功 |
| 邊界／空值 | 空集合、`null`、`0`、剛好等於門檻的值會不會出錯 |
| 明確的錯誤輸入 | 格式不對時是回預設值還是丟例外——而且這是刻意決定的 |

「滿 1000 打九折」這種規則，**剛好等於 1000** 就是一定要有一條測試釘死的邊界。

### 一個指令、寫進文件、而且真的是綠的

測試必須能一個指令跑完，且那個指令要寫在專案文件裡。沒有統一入口的測試，實質上等於沒有測試——下次沒有人（包含 agent）會記得怎麼跑它。分離架構本來就是兩個入口，這是正常的，不必勉強合成一個。

「全綠」要真的驗證過。**測試寫了但沒人跑、跑起來是紅的，比沒有測試更糟——它讓人以為有保護。**

### 不追覆蓋率數字

每一層的目標都是「每條業務規則至少有一個具體斷言」，不是 80%。追高覆蓋率會逼你為膠水碼、欄位宣告、樣板寫沒有意義的測試，那些測試日後只會變成改碼時的阻力。

**測試是要維護的負債。** 覆蓋率從 60% 推到 90% 的那 30%，通常是最沒價值、最難維護的部分。當你發現「再加測試也想不出什麼輸入會讓它出錯」時，這一層就夠了，往下一層走。

## 進度

本頁與子頁採漸進撰寫，一次展開一層。目前兩端都完成**第一層（單元測試）**；下一層是框架內測試——前端掛載元件驗互動，後端跑 `Feature` 打 API endpoint、碰資料庫。

## 術語中英對照

查閱用附錄。**本表涵蓋範圍刻意比正文廣**——mutation、PBT、metamorphic 這些手段在正文還沒展開（它們屬於後面的階段），但查英文資料時會撞到，先把詞對起來。

**標 ★ 的是本頁的用法，不是通用術語**，拿去跟別人溝通前要知道差別。

### 靜態檢查（不執行程式碼）

| 英文 | 中文 | 備註 |
|---|---|---|
| Static Analysis | 靜態分析 | |
| Type Checking | 型別檢查 | TypeScript、PHPStan／Larastan |
| Lint / Linter | 程式碼檢查 | 實務上多直接說「lint」不譯 |

### 按射程分層

| 英文 | 中文 | 備註 |
|---|---|---|
| Unit Test | 單元測試 | |
| Integration Test | 整合測試 | |
| Component Test | 元件測試 | 前端語境 |
| Feature Test | 功能測試 | **Laravel 專有命名**；其他框架多半叫 integration，跟別人講可能被理解成 BDD 的功能測試 |
| — | 框架內測試 ★ | 本頁第 2 層的說法；標準術語接近 integration／component |
| Contract Test | 契約測試 | |
| Consumer-Driven Contract (CDC) | 消費者驅動契約 | 前端是 consumer、後端是 provider |
| End-to-End (E2E) | 端到端測試 | |
| Acceptance Test | 驗收測試 | |
| Smoke Test | 冒煙測試 | 上線後跑一小批確認沒有全爛 |
| Regression Test | 回歸測試 | 指目的，不是某種技術 |

### 判準來源（oracle）

| 英文 | 中文 | 備註 |
|---|---|---|
| Test Oracle | 判準 ★／測試預言 | 學術中譯多為「預言」「判定準則」；查英文資料的關鍵字是 `test oracle` |
| Assertion | 斷言 | |
| Snapshot Testing | 快照測試 | |
| Visual Regression Testing (VR) | 視覺回歸測試 | 與 snapshot 同屬 golden 類 |
| Golden / Approval Testing | 黃金檔案測試／核可測試 | 判準＝先前產出的輸出 |
| Characterization Test | 特徵化測試 | Michael Feathers 用語，遺留系統常用 |
| Baseline | 基準 | VR 語境指「上次那張截圖」 |
| Property-Based Testing (PBT) | 性質測試／屬性測試 | 兩種譯名都常見 |
| Invariant | 不變量 | PBT 的判準本體 |
| Metamorphic Testing | 蛻變測試 | 中文文獻慣用「蛻變」 |
| Metamorphic Relation (MR) | 蛻變關係 | |
| Differential Testing | 差分測試／差異測試 | cross-browser 檢查是它的應用 |
| Fuzzing / Fuzz Testing | 模糊測試 | |
| Crash Oracle | 崩潰判準 | 學術上又稱 no-oracle：唯一標準是「不該崩」 |
| Accessibility Testing (a11y) | 無障礙測試 | 判準來自 WCAG，權威在實作之外 |

### 驗測試本身

| 英文 | 中文 | 備註 |
|---|---|---|
| Mutation Testing | 變異測試／突變測試 | 工具：Stryker（JS）、Infection（PHP） |
| Mutant | 變異體 | 被故意改壞的那份程式碼 |
| Surviving Mutant | 存活變異體 | 改壞了但測試沒叫 ＝ 測試有洞 |
| Mutation Score / MSI | 變異分數／變異測試指標 | 作為品質權威指標**方法論上不成立**，理由見 [[用測試約束-AI-產碼]] |

### 模型與常見病徵

| 英文 | 中文 | 備註 |
|---|---|---|
| Test Pyramid | 測試金字塔 | 單元最多 |
| Testing Trophy | 測試獎盃 | Kent C. Dodds 提出，地基是靜態分析、主張整合測試優先——與金字塔的「單元優先」有張力 |
| Coverage | 覆蓋率 | |
| Flaky Test | 不穩定測試 | 同一份程式碼有時綠有時紅 |
| Test Double | 測試替身 | Mock／Stub／Spy／Fake 的統稱，個別名詞實務上不譯 |
| Tautological Test | 同義反覆測試 | 用實作反推期望值，永遠不會失敗 |
| System Under Test (SUT) | 受測系統 | |
| Reward Hacking | 獎勵鑽營 | AI 語境：優化「測試通過」而非「修好 bug」，見 [[用測試約束-AI-產碼]] |

## 關聯

- [[專案測試流程-前端-Vue]] — 本頁分層與紀律在 Vue 端的落地：邏輯從 `script setup` 抽到 `utils`／composable、Vitest 的檔案落點，以及「測試檔裡出現 `mount()`」這個第 2 層的訊號。
- [[專案測試流程-後端-Laravel]] — 同一套紀律在 Laravel 端的落地：邏輯從 Controller 抽到 Service、`Unit`／`Feature` 兩個 testsuite 正好對應第 1／2 層，以及「`--testsuite=Unit` 跑起來很慢」這個偷連資料庫的訊號。
- [[用測試約束-AI-產碼]] — 本頁是該頁的執行面。該頁回答「為什麼」與「證據多強」（AI 產測試的四種失效模式、TDD 的真正價值、各手段的強度分佈），本頁回答「照什麼順序做」。上面「斷言要具體」要避開的弱斷言與同義反覆，機制說明在該頁第一節。
- [[不讀碼時該看哪些圖]] — 本頁四層射程的**視覺對照**：該頁主張的 container 圖恰好把本頁第 3 層（契約測試）要守的那條 HTTP 邊界畫成一條箭頭——前端等 `total`、後端回 `total_amount` 這類只有契約層抓得到的落差，就發生在圖上前端 → API 那條線上。該頁另補一個本頁沒有的維度：測試管行為、依賴規則管結構，兩者都綠才算數。
- [[AI-產碼加速下的-review-瓶頸]] — 動手建測試流程**之前**該先跑該頁那個成本近零的自我檢驗（數一下已 review 完但未部署的變更），確認瓶頸真的在測試這一側；若積壓其實在部署端，這裡的投入回報有限。
