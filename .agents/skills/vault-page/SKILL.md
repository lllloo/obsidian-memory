---
name: vault-page
description: 把 wiki 某個主題改寫成人類好讀的自包含 HTML 讀本，產出到 `artifacts/`。wiki 頁是給 agent 讀的高密度格式（超長段落、逐條證據標註、勿引用清單），人讀起來吃力；本 skill 針對指定主題把交叉引用密集的一組 wiki 頁合成一頁長文，讀完不必回 wiki。使用時機：使用者說「把 X 寫成好讀的頁」「這主題整理成一頁」「wiki 的 X 讀不下去」「幫我做一頁可以回看的」，或直接呼叫 /vault-page <主題>。不用於畫單張架構圖（那用 /map）、不用於回答問題（那是 Query）。
---

# Vault Page

把 wiki 的一組主題頁，改寫成**一頁自包含的長文 HTML**。

分工前提：wiki 維持給 agent 讀的高密度，**不為人類可讀性妥協**；人讀 artifact。所以這頁的成功標準是「讀完不必回 wiki」，不是「摘要得夠短」。

## 產出

- `artifacts/<主題-kebab>.html`——自包含讀本，檔名用英文 kebab-case，與既有頁一致。
- `artifacts/01.index.html`——所有讀本的入口，每次產頁後就地更新（標題／一句摘要／快照日期）。
- 兩者皆屬呈現層，不參與 Ingest／Query／Lint，不是 `raw/`／`wiki/` 的來源。

## 主流程

1. 用 harness-native `Read schema/vault-map.md` 確認 cwd 是 vault root；讀不到就停止，請使用者 cd 到 vault root（`~/code/obsidian-memory`；三平台一致，cmd.exe 不認 `~` 改用 `%USERPROFILE%\code\obsidian-memory`）。
2. 讀 `wiki/01.index.md`，依使用者給的主題找出**彼此交叉引用密集的一組頁**——邊界是「一條脈絡」而非一個檔案，通常 2–5 頁。
3. **先提範圍給使用者確認**：列出打算涵蓋哪幾頁、分哪幾節，點頭才動筆。自包含讀本一頁一三千字起跳，範圍框錯等於整頁白寫，這是最便宜的檢查點。
4. 讀那組 wiki 頁**全文**（不是只讀 index 摘要），依下方改寫規則寫成 HTML。
5. 更新 `artifacts/01.index.html`；若該主題的來源 wiki 頁尚未指向此頁，在頁內補一行指向（`artifacts/<檔名>.html`）。
6. 回覆附可點的 `file://` 連結。

產出頁面的機制面（落點、可點連結、完整 HTML 的寫法、設計品質校準、inline SVG 畫法）依 `map` skill 的「產出成頁面」一節，本檔不重述。

## 改寫規則

wiki → 讀本的轉換就是這個 skill 的實質內容，四條：

**自包含。** 主體內容完整寫進頁面，不寫「詳見 wiki 某頁」把讀者踢回去。可以連回 wiki 當出處，但那是註記、不是內容的續集。

**證據分層。** wiki 逐條標註強度，全搬過來正是難讀的來源之一，全刪又會讓已經查證到否決的數字重新流回視野。所以：

- 強證據（一手、原始碼、同儕審查）**直接當事實寫，不加註**——讀者看到沒標註就可以直接信。
- 只有**弱的才就地標**：單一作者經驗值、未同儕審查的 preprint、廠商自評、單次實測。
- 被查證**否決**的主張集中到頁尾一節（如「這些數字別引」），列明主張與為何不可用；不無聲丟棄。

**配圖要自己掙位置。** 長文為主。只有真正存在**空間關係或流程關係**的節才畫圖，一頁約 1–3 張；概念性內容（取捨、證據、反例）畫不成圖，用表格、小標與重點框攤開。不為了風格統一而湊圖——把清單畫成方框是零資訊增量。

**快照，不追同步。** 頁尾記來源 wiki 頁清單與產生日期（「依據 wiki 於 YYYY-MM-DD 的內容」）。來源頁之後被 ingest 改了**不回頭修補**，要更新就整頁重跑重生。artifact 是週期性產物，不是活維護層。

## 導覽

每頁頂部導覽列只指回 `artifacts/01.index.html`，不列出其他所有頁——否則每新增一頁都要回頭改完所有舊頁，而舊頁是快照、不該為了導覽被動到。

`01.index.html` 是唯一需要在每次產頁時更新的既有檔。
