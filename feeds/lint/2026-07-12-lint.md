---
title: "2026-07-12 Vault Lint"
created: 2026-07-12
updated: 2026-07-12
tags:
  - lint
---

## 摘要

| 類別 | 數量 |
|---|---|
| 死連結（DEADLINK） | 2（同一連結出現於 2 處，1 個唯一目標） |
| 孤立頁（ORPHAN） | 0 |
| frontmatter 缺欄（FM） | 0 |
| tag 疑似同義異寫 | 0 |
| raw 待消化（clippings，僅彙總） | 5 |
| raw 待消化（fetched，逐條） | 0 |
| 語意層審查頁數 | 1（`wiki/Hermes-Agent.md`，未截斷） |
| 語意層發現 | 3（中 1、低 2） |
| 已自動修補 | 0 |

## 已自動修補

無。本輪唯一的死連結目標檔案雖確實存在（`feeds/youtube/AILABS-393/Hermes-Agent-Token成本優化設定.md`），但位於系統外的 `feeds/`，依 CLAUDE.md 架構 wiki 不得引用 feeds 內容，故不判定為「路徑/名稱寫錯可唯一對應」而自動改連，改列機械層待人工判斷。

## 機械層發現

- **DEADLINK** `wiki/Hermes-Agent.md` → `[[Hermes-Agent-Token成本優化設定]]`（第 69、83 行共 2 處）：wiki／raw 中皆無此檔，唯一同名檔案落在 `feeds/youtube/AILABS-393/`（YouTube 同步筆記，系統外、不供 wiki 引用）。代表這條「token 成本拆解」原始資料實際上從未落地 raw 或 wiki——目標頁正文承諾的細節目前無正式來源可回查。建議：若該筆記內容值得保留為 wiki 依據，應另行走 ingest 流程摘要進 wiki 或 raw；否則移除該 wikilink 或改為一般文字說明「細節待補」。
- **TAG 盤點**：19 個 tag，未發現明顯同義異寫（單複數、連字號變體、語意重疊拼寫）配對；`ai-agent`／`agent-framework`／`coding-agent` 三者語意相近但指涉不同面向（agent 實體／建構框架／coding 專用），判斷為合理共存，非漂移。
- **RAWGAP**：`raw/clippings/` 尚有 5 篇未被任何 wiki 頁引用（僅彙總數量，不主動消化）；`raw/fetched/` 無待消化缺口。

## 語意層發現

範圍：近 7 天有 git 變動的 wiki 頁僅 `wiki/Hermes-Agent.md` 1 頁，連同其出連 5 頁、入連 5 頁（Agent-Harness-Engineering-框架綜述、Claude-Code-記憶系統六層比較、LLM-Wiki-生態實作比較、LLM-Wiki-知識管理模式、第二大腦方法論比較）一併審查。

- `[中] 矛盾` | `wiki/Hermes-Agent.md` | 「Autonomous skill creation…預設免人工核准即可寫入」讀起來暗示 Hermes 整體治理高度自主，但鄰接頁 `LLM-Wiki-生態實作比較.md` 明確定性 Hermes 的 `llm-wiki` skill「全面較保守」（矛盾交使用者複核、10+ 頁大改動先問）。兩者分屬不同子系統（skill 生成 vs. llm-wiki 內部 wiki 治理），但目標頁未點出此區分，讀者可能誤判 Hermes 整體自主權邊界。建議在該列或 `llm-wiki` skill 該列補一句釐清範圍。（低信心，屬措辭層面的潛在誤讀，非確鑿事實衝突）
- `[低] 引用缺口` | `wiki/Hermes-Agent.md` | 關聯區列舉 `llm-wiki` 治理機制時提到 sha256 漂移偵測、封閉 tag taxonomy、矛盾交使用者複核，但漏了鄰接頁 `LLM-Wiki-生態實作比較.md` 同段落提到的「每頁至少 2 條 outbound link」規則；可選擇性補上使清單完整，非必要。
- `[低] 過時` | `wiki/Hermes-Agent.md` | 「Tools（60+）」「官方稱 18–20+ 平台」「宣稱 300+ models」「90+ 預裝 skill」等數字皆為快照值，卻未比照同頁「2,200／1,375 字元上限」與「max turns 150（版本可變預設值）」加註時間/版本抗性標註；建議統一補註記或改用較模糊措辭，交讀者回查官方文件。

已核對但無問題：「`hermes claw migrate` 自 OpenClaw 匯入」目標頁關聯區本身已含此細節（與 `Claude-Code-記憶系統六層比較.md` 一致），非缺口；`max turns 150` 與有界記憶字元數兩處版本快照數字已妥善標註來源與時間限制。

## 建議修補清單

1. `[高／機械]` `wiki/Hermes-Agent.md`：`[[Hermes-Agent-Token成本優化設定]]` 死連結（2 處）——目標僅存於 feeds、不可自動連，需人工決定補 ingest 或移除連結。
2. `[中／語意]` `wiki/Hermes-Agent.md`：釐清「Autonomous skill creation 免核准」與「llm-wiki 治理保守」的範圍區分，避免讀者誤判 Hermes 自主權邊界。
3. `[低／語意]` `wiki/Hermes-Agent.md`：關聯區補「每頁至少 2 條 outbound link」規則，使 `llm-wiki` 治理機制清單與鄰接頁一致完整。
4. `[低／語意]` `wiki/Hermes-Agent.md`：「Tools（60+）」等多處快照數字補時間/版本抗性標註或改模糊措辭。
