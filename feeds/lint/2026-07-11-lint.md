---
title: "2026-07-11 Vault Lint"
created: 2026-07-11
updated: 2026-07-11
tags:
  - lint
---

## 摘要

| 類別 | 數量 |
|---|---|
| 死連結（DEADLINK） | 2（同一目標，出現於 1 檔 2 處）|
| 孤立頁（ORPHAN） | 0 |
| frontmatter 缺欄（FM） | 0 |
| tag 疑似同義異寫漂移 | 0 |
| raw 待消化（RAWGAP） | 5（皆 clippings，僅彙總） |
| index 漏登（INDEXGAP） | 0 |
| 語意層審查頁數 | 1（無截斷） |
| 已自動修補 | 0 |

掃描範圍：wiki + raw（26 檔），語意層近 7 天變動頁 1 篇。cards/topics/feeds 未掃描。

## 已自動修補

無。本輪 DEADLINK 無法唯一對應機械修補（見下）；INDEXGAP 無發現。

## 機械層發現

### 死連結（未自動修，需人工判斷）

- **`wiki/Hermes-Agent.md`**（第 69、83 行）：wikilink `[[Hermes-Agent-Token成本優化設定]]` 指向的頁在 `wiki/` 與 `raw/` 皆不存在——實際內容躺在 `feeds/youtube/AILABS-393/Hermes-Agent-Token成本優化設定.md`。依 `CLAUDE.md`，`feeds/` 不屬三層系統、**不得作為 raw 或 wiki 的來源**，因此不是「路徑寫錯可唯一對應」的情況，不做機械修補。這兩處把該 YouTube 影片筆記當成與 `[[Hermes-Agent-NousResearch]]`、`[[Hermes-Agent-Kanban]]` 同級的「原始資料」引用，但那兩者都已正式落地 `raw/fetched/`，只有這筆漏了。建議二選一：(a) 把該影片內容依 CLAUDE.md 的貼 URL ingest 流程正式收進 `raw/fetched/`，把 wikilink 指過去；或 (b) 若內容已被本頁其餘章節涵蓋、無需獨立 raw 頁，移除這兩處連結、改成一般文字提及或 inline 連結。

### 孤立頁 / frontmatter / index 漏登

無發現。

### Tag 盤點（同義異寫疑似漂移）

掃到 19 個 tag（`clippings:10`、`knowledge-graph:6`、`ai-agent:5`、`obsidian:4`、`wiki:4`、`agent-framework:3`、`claude-code:3`、`coding-agent:3`、`context-engineering:3`、`index:3`、`pkm:3`、`second-brain:3`、`memory:2`、`rag:2`、`automation:1`、`best-practices:1`、`llm-pricing:1`、`mcp:1`、`skill:1`）。逐一核對後**無明顯同義異寫漂移對**：`pkm`／`second-brain` 三頁皆成對出現（不同粒度、非互斥同義詞）；`ai-agent`／`agent-framework`／`coding-agent` 是同一叢集下的不同切面（廣義類別／框架特性／編程應用），各頁依內容取用不同子集，非漂移。

### Raw 待消化缺口

- `raw/clippings/`：5 筆待消化（僅彙總數量，不逐條列）：
  - AI-做的設計，你能看出哪裡有問題嗎？
  - Copilot-的程式碼品質堪憂？GitHub-官方-spec-kit-登場，用規格驅動開發取代-Vibe-Coding
  - The-Official-BMad-Method-Masterclass-(The-Complete-IDE-Workflow)
  - Harness-Engineering-到底是什麼？概念、實戰與爭議，一次全部講清楚
  - Claude-Code-Best-Practice-—-Threads-Carousel-Cards
- `raw/fetched/`：0 筆未被引用（全部已被 wiki 頁引用）。

## 語意層發現

審查頁：`wiki/Hermes-Agent.md`（近 7 天內唯一變動頁），鄰接頁：`raw/fetched/Hermes-Agent-NousResearch.md`、`raw/fetched/Hermes-Agent-Kanban.md`、`wiki/LLM-Wiki-知識管理模式.md`、`wiki/LLM-Wiki-生態實作比較.md`、`wiki/Claude-Code-記憶系統六層比較.md`、`wiki/第二大腦方法論比較.md`、`wiki/Agent-Harness-Engineering-框架綜述.md`。

無發現 | `wiki/Hermes-Agent.md`

- **矛盾**：無。頁內「更正」區塊（2026-07-09 deep-research 對抗式驗證駁回舊主張）與 `raw/fetched/Hermes-Agent-NousResearch.md` 的原始描述並存但已就地標註取代關係，非矛盾。與各鄰接頁交叉比對（OpenClaw 血緣、pkm/second-brain 對照、harness 構件對照）皆一致。
- **過時／時間脆弱主張**：無新增問題。版本相關數字（記憶上限字元數、max turns 預設值）已標「官方文件快照值，隨版本可變」，符合時間抗性慣例。
- **交叉引用缺口**：無。與 7 個鄰接頁的連結皆已雙向（`wiki/Hermes-Agent.md` → 鄰接頁、鄰接頁 → `wiki/Hermes-Agent.md` 逐一確認成立）。

## 建議修補清單

1. `wiki/Hermes-Agent.md`（第 69、83 行）：`[[Hermes-Agent-Token成本優化設定]]` 死連結指向 feeds 而非 raw/wiki——需人工決定補 raw 頁或移除連結（高）。
2. `raw/clippings/` 有 5 筆待消化原料，可挑選排入下一輪 ingest（低，僅供參考）。
